thread_def Child {
  receive {
    (int x, string y) -> x = 0;
    _ -> {}
  }
}

thread_def Main {
  (int, string) tup = (1, "hello");
  thread t = spawn Child;
  t << tup;
  (int x2, string y2) = tup;
  print(string_of_int(x2) + "\n");
}

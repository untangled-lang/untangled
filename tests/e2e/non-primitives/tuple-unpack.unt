thread_def Main {
  string newline = "
";

  ((int, string), (int, int)) b =  ((2, "hello"), (9, 3));
  b = ((1, 9), (2, 3));
  ((int x, string a), (int y, int z)) = b;


  print(a + newline);
  print(string_of_int(x) + newline);
  print(string_of_int(y) + newline);
  print(string_of_int(z) + newline);
}
void foo((int, float) tup) {
  (int i, float f) = tup;
  i += 100;
  return;
}

thread_def Main {
  (int, float) tup = (0, 0.0);
  foo(tup);
  (int i, float f) = tup;
  print(string_of_int(i) + "\n");
}

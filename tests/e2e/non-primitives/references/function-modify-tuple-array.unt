void foo((int[3], float) tup) {
  (int[3] arr, float f) = tup;
  arr[0] += 10;
  return;
}

thread_def Main {
  (int[3], float) tup = ([0, 0, 0], 0.0);
  foo(tup);
  (int[3] arr, float f) = tup;
  print(string_of_int(arr[0]) + "\n");
}

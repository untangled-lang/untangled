thread_def Child {
  receive {
    int x -> {
      print(string_of_int(x));
    }
    _ -> {}
  }
  parent << "done";
}

thread_def Main {
  thread t = spawn Child;
  t << 100;
  receive {
    _ -> {}
  }
}

thread_def Main {
  string newline = "
";
  int base = 5;
  print(string_of_int(base ** 0) + newline);
  print(string_of_int(base ** 1) + newline);
  print(string_of_int(base ** 5) + newline);
  print(string_of_int(base ** -1) + newline);
  print(string_of_int(base ** -3) + newline);
}

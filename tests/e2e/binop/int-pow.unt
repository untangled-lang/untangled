thread_def Main {
  string newline = "
";
  int base = 5;
  print(string_of_int(0 ** 0) + newline); // 1
  print(string_of_int(base ** 0) + newline); // 1
  print(string_of_int(base ** 1) + newline); // 5
  print(string_of_int(base ** 5) + newline); // 3125
  print(string_of_int(base ** -1) + newline); // 0
  print(string_of_int(base ** -3) + newline); // 0
  print(string_of_int(1 ** -1) + newline); // 1

  int neg = -3;
  print(string_of_int(neg ** 0) + newline); // 1
  print(string_of_int(neg ** 1) + newline); // -3
  print(string_of_int(neg ** 3) + newline); // -27
  print(string_of_int(neg ** 4) + newline); // 81
  print(string_of_int(neg ** -1) + newline); // 0
}

thread_def Child {
  receive {
    string x -> print(x);
    (string x, string y) -> print("x: " + x + " y: " + y);
    _ -> print("bad");
  }
}

thread_def Main {
  thread t = spawn Child;
  t << ("hello", "bruh");
}

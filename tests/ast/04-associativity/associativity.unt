thread_def Main {
  int a = (25 - 10) - 5;
  int b = 25 - 10 - 5;
  int c = 25 - (10 - 5);

  int d = 25 - 10 + 2 * 3 / 2 * 6 % 4;
  int e = 25 - (10 + 2) * 3 / (2 * 6) % 4;

  a++;
  a--;

  a += b += c;

  bool f = a == b;
  bool g = a != b;
  bool h = a != b && b == c;
  bool i = a == b && b != c;
  bool j = a == b == c == d;
  bool k = (a == b) == (c == d);
  bool l = a > b;
  bool m = a < b;
  bool n = a >= b > c;
  bool o = a > b >= c;
  bool p = a <= b == c;
  bool q = a == b <= c;
  bool r = a <= b == c >= d;
}
